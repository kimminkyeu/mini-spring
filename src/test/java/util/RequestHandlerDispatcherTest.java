package util;

import minispring.DispatcherManagerProvider;
import minispring.RequestHandler;
import org.junit.Test;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.util.Optional;

import static org.assertj.core.api.Assertions.*;

public class RequestHandlerDispatcherTest {
  private static final Logger logger = LoggerFactory.getLogger(UrlToRegexUtilTest.class);

  @Test
  public void getParamFromRegexTest() {

  }
}
